import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const sellerAccessSlice = createSlice({
	name: "sellerAccess",
	initialState,
	reducers: {
        // Add reducers
	},
});

export const {} = sellerAccessSlice.actions;

export default sellerAccessSlice.reducer;
