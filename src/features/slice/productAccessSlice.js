import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const productAccessSlice = createSlice({
	name: "productAccess",
	initialState,
	reducers: {
        // Add reducers
	},
});

export const {} = productAccessSlice.actions;

export default productAccessSlice.reducer;
